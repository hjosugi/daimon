{
  "buildCommand": "pnpm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install -g pnpm@9.15.0 && pnpm install --no-frozen-lockfile",
  "framework": "vite",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ./frontend"
}
